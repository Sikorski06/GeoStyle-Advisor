apiVersion: v1
kind: Service
metadata:
  name: geostyle-service
spec:
  type: LoadBalancer
  sessionAffinity: ClientIP
  selector:
    app: geostyle-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8501